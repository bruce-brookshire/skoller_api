# Always rebuild from scratch on every deploy?
always_rebuild=true

# Erlang version
erlang_version=20.0

# Elixir version
elixir_version=1.5.2

config_vars_to_export=(MIX_ENV SECRET_KEY_BASE DATABASE_URL)